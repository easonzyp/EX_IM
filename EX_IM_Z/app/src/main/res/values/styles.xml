<resources>
    <!--
            Base application theme, dependent on API level. This theme is replaced
            by AppBaseTheme from res/values-vXX/styles.xml on newer devices.

        -->
    <style name="AppBaseTheme" parent="android:Theme.Light">
        <!--
            Theme customizations available in newer API levels can go in
            res/values-vXX/styles.xml, while customizations related to
            backward-compatibility can go here.

        -->
    </style>

    <!-- Application theme. -->
    <style name="AppTheme" parent="AppBaseTheme">

        <!-- All customizations that are NOT specific to a particular API-level can go here. -->
        <item name="android:windowNoTitle">true</item>
    </style>

    <style name="postfrom">
        <item name="android:textSize">11dp</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textStyle">italic</item>
    </style>



    <style name="main_tab_bottom2">
        <item name="android:ellipsize">marquee</item>
        <item name="android:gravity">center_horizontal</item>
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:button">@null</item>
        <item name="android:singleLine">true</item>
        <item name="android:layout_weight">1.0</item>
    </style>


    <style name="my_dialog" parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item> <!-- 去除边框 -->
        <item name="android:windowIsFloating">true</item> <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">false</item> <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item> <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item> <!-- 自己想要的背景 -->
        <item name="android:backgroundDimEnabled">true</item> <!-- 背景遮罩层 -->
    </style>


    <style name="ViewPagerButtonUnselected">
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/gray</item>
        <!--<item name="android:clickable">true</item>-->
    </style>

    <style name="HPViewPagerButtonUnselected">
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/hp_title_txt_color</item>
        <!--<item name="android:clickable">true</item>-->
    </style>
    <style name="ViewPagerButtonUnselectedNew">
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/new_nlp_title_color</item>
        <!--<item name="android:clickable">true</item>-->
    </style>
    <style name="ViewPagerButtonUnselectedNewLogin">
        <item name="android:background">@null</item>
        <item name="android:textColor">@color/title_color_new</item>
        <!--<item name="android:clickable">true</item>-->
    </style>
    <style name="titleTheme" parent="android:Theme">
        <item name="android:windowTitleSize">@dimen/title_height</item>
        <item name="android:windowContentOverlay">@android:color/transparent</item>
        <item name="android:windowTitleBackgroundStyle">@style/titleBackground</item>
    </style>

    <style name="titleBackground">
        <item name="android:background">@color/titleBarBackground</item>
    </style>
</resources>
